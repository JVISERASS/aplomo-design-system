Gestor de columnas, anclado al IconButton "Columnas".

```jsx
<ColumnManager open={open} onClose={close} hidden={hidden} onToggle={toggle}
  columns={[{key:"ref",label:"Referencia",locked:true},{key:"region",label:"Region"},{key:"ver",label:"Version"}]} />
```
