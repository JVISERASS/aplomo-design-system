Envuelve TODO dato. Es el elemento firma del sistema: MONO 1, tabular-nums, y cuaja desde MONO 0 en 250ms al cargar o cambiar.

```jsx
<DataValue value="99,982" loading={pending} />
<DataValue value="SVC-4820-DE" size={13} tone="muted" />
```

Nunca uses DataValue para prosa, etiquetas ni titulos: esos van en MONO 0.
