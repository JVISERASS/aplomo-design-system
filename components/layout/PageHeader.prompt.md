Cabecera de vista. Titulo siempre en sentence case, nunca Title Case.

```jsx
<PageHeader eyebrow="Infraestructura" title="Servicios en produccion" meta={<DataValue value="248 servicios · 12 regiones"/>} actions={<Button variant="primary">Nuevo servicio</Button>} />
```
