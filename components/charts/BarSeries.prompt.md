Serie temporal de una metrica. Acompana siempre a una cifra grande en `DataValue`, nunca va sola.

```jsx
<BarSeries data={rpm24h} labels={["hace 24 h","ahora"]} threshold={80} loading={loading} />
```
