Paleta de comandos. El atajo global lo registra la pantalla; el componente gestiona ↑↓ / ↵ / Esc.

```jsx
<CommandPalette open={open} onClose={close} onSelect={go}
  items={[{id:"s1",label:"SVC-4820-DE",hint:"eu-central-1",data:true,icon:"server"},{label:"Drenar trafico",hint:"accion",icon:"activity"}]} />
```
